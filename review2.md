# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
- <input type="hidden" name="_method" value="PUT">が出力されている

- HTMLの <form> タグはGETとPOSTにしか対応していない
- しかし、Laravel のルーティングでは、リソース更新にはPUTやPATCH、削除にはDELETEを使うのが基本
- @method('PUT')と記載することで、POST送信として中に"PUTを使いたい"という情報を隠して送れるようにしている


### findメソッドの引数に指定しているIDは何のIDか
- todosテーブルのプライマリーキー


### findメソッドで実行しているSQLは何か
- $todo = $this->todo->find($id);
- 実行しているSQLは、SELECT id FROM todos; 


### findメソッドで取得できる値は何か
- Todoモデルのインスタンス

- findメソッドは、LaravelのEloquentモデルで使われるメソッドで、
  主キー(通常は id)を指定して、1件のレコードを取得するために使う


### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
- save()メソッドはモデルの内容をDBに保存する
- IDが無ければINSERT IDがあればUPDATE

- $todoが新しいインスタンスであれば新規登録(新しい行を追加) → INSERT INTO を実行
  送られてきたデータが、ユーザーから送信されてきたデータ
- $todoが既存レコードを取得したものであれば更新(既存の行を上書き) → UPDATE ... WHERE id=? を実行
  送られてきたデータがデータベースから送られてきたデータ


## Todo論理削除

### traitとclassの違いとは
- トレイトとは、クラスにプロパティやメソッドを追加するための機能
  これにより複数のクラス間でコードを共通化・再利用することが可能になる

- クラスはインスタンスを生成するための設計図であり、
　トレイトはコードの再利用を目的とした機能の詰め合わせのようなもの
　クラスは継承できるが、トレイトはインスタンス化できないため、
  useしてクラスに混ぜ込む形で使用する

- トレイトはtrait{トレイト名}で定義し、クラスにuse{トレイト名}とすることで使用できる


### traitを使用するメリットとは
- 同じ処理を何度も書かずに、traitを使って共通化できるため、コードの再利用ができる。
  加えて、PHPはクラスの多重継承はできないが、トレイトなら複数組み合わせて使用できるため、
  いくつもの便利な機能を、必要なクラスに自由に付け足すことができる。
  また、共通処理が1か所にまとまっているため、一括修正ができ、メンテナンス性の向上が見込まれる。


## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
-     public function __construct(Todo $todo)
      {
        $this->todo = $todo; →ここで実行される
      }

- __construct()は、コントローラなどのクラスが初めて使用される時に1回だけ実行される初期処理


### RequestクラスからFormRequestクラスに変更した理由
- バリデーションルールに関する記述がFormRequestクラスに記載されているため、
  どのリクエスト処理を継承したいか、意図を明確化するために名前を変更


### $errorsのhasメソッドの引数・返り値は何か
- $errors->has('content')

- 引数：'content'
- 返り値：true エラーがある

- $errorsについて
  $errorsはグローバル変数
  $errorsを通してどのBladeからでもセッションに保存されたメッセージを取得できる

- $errorsはMessageBagクラスのインスタンスが代入されており、発生したバリデーションエラーの情報を持っている
- $errors->has('入力欄のname属性')で、その入力欄でバリデーションエラーが発生しているか判定する
  （指定したフィールドにエラーがあるかどうかをチェック　エラーがあればtrueなければfalseを返す）
- $errors->first('入力欄のname属性')で、その入力欄で最初に発生したエラーメッセージを出力している

### $errorsのfirstメソッドの引数・返り値は何か
- $errors->first('content')

- 引数：'content'
- 返り値：エラーメッセージ


### フレームワークとは何か
- アプリケーション開発に必要な基本的な機能や構造をあらかじめ用意してくれている枠組みのこと

- フレームワークを学ぶメリット
  - 容易に一定品質のプロダクトを作成することができる
  - 枠組みが決定しているため初見のアプリケーションでもコードの記述場所などが特定しやすい


### MVCはどういったアーキテクチャか
- MVCとはソフトウェアの設計パターンのひとつで、アプリケーションを役割に分けて整理する仕組み
  - Model       DBとのやり取りを司る（データの管理・保存・取得を担当）
  - View        ユーザ (Client) に表示する画面の生成を司る
  - Controller	ユーザからの入力 (送信データ) に基づき、 Model・Viewの制御を司る
  - Routing	    ユーザからの依頼（リクエスト）に応じてどの処理を動かすかの判断を司る

- MVCとは開発効率を高めるために作られたアーキテクチャで再利用性・可読性を優れ、保守性を高めてくれる
  MVCごとに役割が分担されており、それぞれの役割を理解することが重要である

- アーキテクチャとは、システムやソフトウェアの全体的な構造や設計方針のことを指す


### ORMとは何か、またLaravelが使用しているORMは何か
- ORM (Object-Relational Mapping) 
- ORMとは、プログラミング言語のClassとデータベースのテーブルをマッピング（関連付け）すること
  SQLを直接操作することなくデータベースとマッピングされたClassのメソッドを用いることでDBとやり取りを行うことができる


### composer.json, composer.lockとは何か
- composer.jsonは、Composerが使う設定ファイルで、
  プロジェクトがどんなライブラリ（パッケージ）を使っているかを記述するためのファイル

- PHPのパッケージ管理ツールComposerが生成する依存関係の正確なバージョンを記録したファイル
  composer.jsonに書かれたバージョン指定はあいまいであるため、実際にインストールされた正確なバージョンを記録する


### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
- 基本的にプロジェクトのルートディレクトリにあるvendor/フォルダに保存される